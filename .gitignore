# Dependencies
node_modules/

# Build output
dist/
build/

# 渲染产物（HTML 文件，含嵌套目录）
output/

# 狗食验收产物（brickify / refactor_judge 等工具跑出的 HTML/JSON 报告，不入库）
.artifacts/

# 布局腐坏备份 / 诊断脚本等临时产物
.backup/

# 一次性临时脚本
scripts/*.tmp.mjs

# 运行时持久化的 DSL（用户实际使用时生成；examples/ 目录的示例需要提交）
.design-canvas/
# MCP server 运行时 DSL（项目根，随使用场景变化，不入库）
design-canvas.json
# Camera DSL 仓库（运行时生成：{projectRoot}/.agent/camera/dsl.json + history）
.agent/
# Go 编译产物
*.exe

# 测试覆盖率
coverage/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 日志
*.log
npm-debug.log*

# 环境变量
.env
.env.local

# 临时文件
*.tmp
*.bak

# 参考库（借鉴用，不属于本项目代码）
vendor/

# agent-shell 插桩实验副本（完整 agent-shell 源码副本，非本项目代码；trace 核心已抽到 go-trace/）
go-lab/

# codegraph 根目录残留（重复拷贝，已移至 vendor/codegraph；剩余 .git/pack 被 IDE 占用，释放后可删）
codegraph/

# ───── 诊断 / 一次性脚本 / 截图（不入仓库） ─────
# 性能诊断脚本
scripts/perf_*.py
scripts/dbg_*.py
scripts/inspect_*.py
# 调试用 mjs
scripts/_dbg_*.mjs
scripts/_diag*.mjs
scripts/_verify_*.mjs
scripts/_repro_*.mjs
scripts/_hit*.mjs
scripts/_inspect_*.mjs
scripts/_diag_*.png
scripts/_drag_*.png
scripts/_hit*.png
scripts/_inspect_*.png
# 截图目录
shots/
# 一次性冒烟 / 拖拽诊断脚本 / 单张现场截图
scripts/*_smoke.mjs
scripts/test_drag_*.py
scripts/_star_full.png
# 根目录一次性调试脚本
_dbg_*.mjs
_tmp_*.ts
test_*.mjs
test_*.ts
test_*.txt

# ───── 发布划界（内部实验/诊断/目标项目专属，不入发布仓库） ─────
# 内部 Go 插桩实验与探针（区分于 svc 的 go-slim/go-camera 运行时依赖）
go-trace/
camera-conformance/
scaffold/
# 内部文档（dev 过程稿：plans/progress/验收纪要/报告/preview html）
docs/
# 内部交接/狗食试产产物
HANDOVER_DOGFOOD.md
# 内部后续开发路径规划（含竞品/许可/变现/缺陷审计等内部结论，不入发布仓库）
ROADMAP.md
.tmp_test.mjs
.brickify_agentshell_report.json
*.report.json
# 一次性/诊断/目标项目专属脚本（产品链 scripts 保留：gen_anim_core_bundle / install_mcp / install_global_skill / promote_design_canvas_mcp / setup / hub / rebuild_feature / gen_teach / rebuild_teach / scan_projects / batch_import / audit_env / reorg_env）
scripts/*_aibase.mjs
scripts/annotate_aibase.mjs
scripts/bughunt_self.mjs
scripts/*_smoke.mjs
scripts/*_verify.mjs
scripts/*_real.mjs
scripts/check_*.mjs
scripts/dogfood_audit.mjs
scripts/e2e_*.mjs
scripts/regen_*.mjs
scripts/regenerate_design.mjs
scripts/relayout_*.mjs
scripts/rerender_output.mjs
scripts/derive_real_aibase.mjs
scripts/derive_algorithm_real.mjs
scripts/dump_geom.mjs
scripts/reorg_env.cjs
scripts/audit_env.cjs
scripts/batch_import.cjs
scripts/scan_projects.cjs
# go-camera 迁移交接文档（本地保留，含个人工程路径，不入发布仓库）
go-camera/MIGRATED_FROM_AI_BASE.md
# 狗食落盘 runner（直接对外部真实项目写盘，clone 后不可运行，不入发布套件）
tests/tools/z_dg_*.test.ts

# 根目录诊断截图 / 一次性校验脚本（验收现场图，不入发布仓库）
/_*.png
/_*.cjs
/_*.py
/L3-*.png
/gw_panel_check.png

